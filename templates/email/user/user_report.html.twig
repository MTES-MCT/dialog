{% extends 'email/base.html.twig' %}
{% trans_default_domain 'emails' %}

{% block body %}
    <h1>{{ 'contact.email.user_report'|trans }}</h1>
    <p>{{ fullName }} {{ email }}</p>
    <p>{{ location }}</p>
    <p>{{ content }}</p>
{% endblock %}
