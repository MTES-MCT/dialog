{%- if location.fromPointNumber is not null and location.fromPointNumber is not same as('') and location.toPointNumber is not null and location.toPointNumber is not same as('') -%}
    {{ 'regulation.location.reference_point'|trans({
        '%fromPointNumber%': location.fromPointNumber,
        '%fromAbscissa%': location.fromAbscissa,
        '%fromSide%': location.fromSide,
        '%toPointNumber%': location.toPointNumber,
        '%toAbscissa%': location.toAbscissa,
        '%toSide%': location.toSide
    }) }}
{%- endif -%}
