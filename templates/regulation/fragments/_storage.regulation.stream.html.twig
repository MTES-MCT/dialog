<turbo-stream action="update" target="upload-form-frame">
    <template>
        {% if redirectUrl is defined %}
            <script>window.top.location = "{{ redirectUrl }}";</script>
        {% else %}
            {% include 'regulation/storage_regulation_order.html.twig' with { uuid } %}
        {% endif %}
    </template>
</turbo-stream>
