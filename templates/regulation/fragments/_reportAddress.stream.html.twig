<turbo-stream action="update" target="create-report-address-form-frame">
    <template>
        {% if redirectUrl is defined %}
            <script>window.top.location = "{{ redirectUrl }}";</script>
        {% else %}
            {% include 'regulation/reportAddress.html.twig'%}
        {% endif %}
    </template>
</turbo-stream>
