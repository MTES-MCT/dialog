{% for result in results %}
    <li role="option" data-autocomplete-value="{{ result.roadNumber }}">{{ result.roadNumber }}</li>
{% endfor %}

<template id="status">
    {{ 'common.autocomplete.results_count'|trans({ '%count%': results|length }) }}
</template>
